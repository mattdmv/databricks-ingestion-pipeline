# The main job for ingestion_pipelines.
resources:
  jobs:
    pipeline_1_trigger_job:
      name: pipeline_1_trigger_job

      trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        file_arrival:
          url: s3://databricks-delta-lake-s3-integration-test/landing/
        pause_status: UNPAUSED

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com

      tasks:
        - task_key: trigger_pipeline_1
          pipeline_task:
            pipeline_id: ${resources.pipelines.pipeline_1.id}

      # A list of task execution environment specifications that can be referenced by tasks of this job.
      environments:
        - environment_key: default

          # Full documentation of this spec can be found at:
          # https://docs.databricks.com/api/workspace/jobs/create#environments-spec
          spec:
            client: "2"
            dependencies:
              - ../dist/*.whl
